// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set your production URL here (used for sitemap, canonical links, etc.)
  site: 'https://example.com',
});
